server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    # Try files, otherwise fallback to index.html
    location / {
        try_files $uri /index.html;
    }

    # Optional: serve static assets efficiently
    location /assets/ {
        try_files $uri =404;
    }
}